[package]
name = "tv-plugin-git"
version = "0.1.0"
edition = "2021"
description = "Bundled comprehensive git plugin for tree-viewer (tv). Provides status, diff, log, and blame."
license = "GPL-3.0-or-later"
repository = "https://github.com/ansromanov/tree-viewer"

[dependencies]
serde_json = "1"

[package]
name = "tv-plugin-git-log"
version = "0.1.0"
edition = "2021"
description = "Bundled git-log plugin for tree-viewer (tv). Shows file commit history on H keypress."
license = "GPL-3.0-or-later"
repository = "https://github.com/ansromanov/tree-viewer"

[dependencies]
serde_json = "1"

[package]
name = "mantis-plugin-git-diff"
version = "0.1.0"
edition = "2021"
description = "Bundled git-diff plugin for mantis. Shows git working-tree diff when opening tracked files."
license = "GPL-3.0-or-later"
repository = "https://github.com/ansromanov/mantis"

[dependencies]
serde_json = "1"
